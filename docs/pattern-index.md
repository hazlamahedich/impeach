# Pattern Index

> Placeholder gate **PH-F1-04**. The Pattern Index (PC-7) cross-references each
> language-level helper (`withTx`, `upsert.ts`, AGE `cypher()` wrapper,
> `@iip/llm-router`, `makeEntry`, `EditorialLog.append`) back to its rule + ADR.
> Content is owned by **Stories 1.10 / 1.12**. This stub exists unconditionally in
> Story 1.1.
