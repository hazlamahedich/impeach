{
  "$schema": "https://raw.githubusercontent.com/StrykerMutator/stryker/master/packages/api/schema/stryker-core.json",
  "_comment": "ACTIVE — Epic 1 post-NFR-audit. packages/render/src/gate.ts exists and has tests. packages/auth/src/verify.ts is deferred to Epic 2 Story 2.2.",
  "packageManager": "pnpm",
  "reporters": ["html", "clear-text", "progress"],
  "testRunner": "vitest",
  "coverageAnalysis": "perTest",
  "concurrency": 1,
  "_concurrency_note": "concurrency:1 for AC-11 hash-chain tests — parallelism masks mutants under concurrent writers",
  "thresholds": {
    "high": 100,
    "low": 100,
    "break": 100
  },
  "mutate": [
    "packages/render/src/gate.ts"
  ],
  "ignorePatterns": [
    "**/._*",
    "**/.agent/**",
    "**/.agents/**",
    "**/.claude/**",
    "**/node_modules/**",
    "**/dist/**"
  ],
  "vitest": {
    "configFile": "packages/render/vitest.config.ts",
    "related": false
  },
  "_mutate_pending": [
    "packages/auth/src/verify.ts!{100}",
    "packages/citation/src/verify.ts!{90}",
    "packages/intake/src/state.ts!{90}",
    "apps/ingest-worker/src/extract/worker.ts!{90}"
  ],
  "_mutate_pending_note": "Files above will be added when they exist. 100% on auth/verify.ts per SEC-8; ≥90% on others."
}
