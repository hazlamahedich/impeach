---
stepsCompleted: ['step-01-preflight-and-context','step-02-generation-mode','step-03-test-strategy','step-04-generate-tests']
lastStep: 'step-04-generate-tests'
lastSaved: '2026-06-22'
workflowType: 'testarch-atdd'
storyId: '1.6'
storyKey: '1-6-citation-package'
generatedTestFiles:
  - 'packages/citation/citation-tuple.test.ts'
---

# ATDD Checklist — Epic 1, Story 1.6: Citation Package (SC-2/AC-4)

**Date:** 2026-06-22 · **Primary Test Level:** unit + contract · **Severity:** **T1 (defamation primitive)**

## Acceptance Criteria
1. Tuple is `(source_doc_id, span_start, span_end, content_hash)` per AC-4
2. Hash algorithm defined per ADR-010 (xxhash OR sha256)
3. `emit(span, source)` and `verify(citation)` APIs exist
4. `packages/citation` NOT coupled into `packages/rag`
5. `CitationTuple` zod schema lives in `packages/contracts`

## Red-Phase Scaffolds
**File:** `packages/citation/citation-tuple.test.ts` (7 tests)

- ⏭️ CitationTuple zod shape — RED
- ⏭️ exact AC-4 field shape (defends drift) — RED
- ⏭️ ADR-010 Accepted with evidence — RED
- ⏭️ emit() produces tuple — RED
- ⏭️ verify() re-derives hash (tamper-detect) — RED
- ⏭️ rag NOT coupled to citation — RED
- ⏭️ content_hash survives re-index — RED (AC-4 essence)

## Implementation Checklist

- [ ] `packages/contracts/src/citation.ts`: `CitationTuple` zod with `sourceDocId: z.string().uuid({version:'v4'}), spanStart: z.number().int().min(0), spanEnd: z.number().int().min(0), contentHash: z.string().brand<'CorpusHash'>()`
- [ ] `packages/citation/src/index.ts`: `emit(span, source)` → `CitationTuple`; `verify(citation, source)` → boolean
- [ ] Hash = `crypto.subtle.digest('SHA-256', canonicalizedSpanText)` (per project-context.md, NOT node:crypto)
- [ ] Brand `CorpusHash` so `corpusHash = prevHash` is a compile error (Winston branded types)
- [ ] ADR-0010 `docs/adr/0010-citation-hash-algorithm.md`: status Accepted, evidence references this test
- [ ] `packages/rag/package.json` does NOT depend on `@iip/citation` (AC-4 decoupling)
- [ ] Activate `test.skip` → GREEN

**Estimated Effort:** 1 day

## Notes
- **Citation is decoupled from embeddings by design (AC-4)** — binds to span/content_hash, not vector. Re-embedding preserves citation validity (Story 4.7 relies on this).
- Produced in `ingest`, attached in `rag`, verified at render gate (Story 2.1), scored in `eval`, preserved across edits by `editorial` (AC-11). NOT coupled into `rag`.
- `emit()`/`verify()` are the API surface that survives the whole pipeline — keep them minimal and stable.

**Generated by BMad TEA Agent** — 2026-06-22
