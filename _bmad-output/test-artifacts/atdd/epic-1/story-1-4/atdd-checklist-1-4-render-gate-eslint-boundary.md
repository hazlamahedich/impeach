---
stepsCompleted: ['step-01-preflight-and-context', 'step-02-generation-mode', 'step-03-test-strategy', 'step-04-generate-tests']
lastStep: 'step-04-generate-tests'
lastSaved: '2026-06-22'
workflowType: 'testarch-atdd'
storyId: '1.4'
storyKey: '1-4-render-gate-eslint-boundary'
generatedTestFiles:
  - 'packages/render/gate-boundary.test.ts'
---

# ATDD Checklist — Epic 1, Story 1.4: Render Gate ESLint Boundary (AC-2)

**Date:** 2026-06-22 · **Primary Test Level:** unit (ESLint API) + contract · **Severity:** **T1 (defamation boundary)**

## Acceptance Criteria
1. `packages/render` imports ONLY `@iip/contracts` (SC-3, AC-F1-08)
2. `packages/rag` banned from importing `@iip/render`
3. `packages/contracts/src/render.ts` defines the `RenderInput` zod schema (only shared symbol)
4. Render gate placeholder throws `RenderViolation` on any claim lacking citation support
5. ESLint boundary verified in CI

## Red-Phase Scaffolds
**File:** `packages/render/gate-boundary.test.ts` (7 tests)

- ⏭️ render imports only contracts — RED — package absent
- ⏭️ rag banned from render — RED — package absent
- ⏭️ RenderInput zod schema — RED — contracts absent
- ⏭️ **gate throws RenderViolation on uncited** — RED — gate.ts absent (AC-2 primitive)
- ⏭️ ESLint no-restricted-paths rule — RED — @iip/eslint-plugin absent
- ⏭️ CI workflow runs boundary check — RED — ci.yml absent
- ⏭️ no @ts-ignore in render/contracts — RED — Winston #5 ban

## Implementation Checklist

- [ ] Create `packages/contracts/src/render.ts` exporting `RenderInput` zod schema (the ONLY shared symbol)
- [ ] Create `packages/render/src/gate.ts` with `gate(input: RenderInput)` that throws `RenderViolation` if any clause has zero citations
- [ ] Scaffold `@iip/eslint-plugin` workspace package with `no-restricted-paths` config:
  - `packages/render/src/**` allowed: `@iip/contracts` ONLY
  - `packages/rag/src/**` banned: `@iip/render`
- [ ] Use `eslint-plugin-import` `import/no-restricted-paths` with `zones: [{ target, from }]` for directional boundaries
- [ ] Add `import/no-relative-packages: error` + `import/no-extraneous-dependencies: error`
- [ ] `package.json` `exports` field on every package (HARD boundary, Winston #9)
- [ ] ESLint flat config: `@iip/eslint-plugin` as `error` in CI
- [ ] Add boundary assertion to `.github/workflows/ci.yml`
- [ ] Ban `@ts-expect-error` + `@ts-ignore` in render/contracts (Winston #5)
- [ ] Activate `test.skip` → GREEN

**Estimated Effort:** 2 days (mostly ESLint plugin authoring)

## Notes
- **This is the first defamation-relevant test in the epic.** AC-2 says hard gates are non-relaxable. The boundary is what makes "render gate" structurally separate from generation — without it, AC-2 is a runtime convention, not a type.
- `package.json` `exports` is the HARD boundary (bundler-enforced), not ESLint (advisory) — Winston #9.
- Gate placeholder need NOT be wired as a live call site in Epic 1 (that's Story 2.1). It just needs to exist and throw on uncited input.
- `RenderViolation` is a discriminated union member in `packages/contracts/src/error.ts` (Winston #17 `AppError` canonical shape).

**Generated by BMad TEA Agent** — 2026-06-22
