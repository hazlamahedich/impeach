---
stepsCompleted: ['step-01-preflight-and-context','step-02-generation-mode','step-03-test-strategy','step-04-generate-tests']
lastStep: 'step-04-generate-tests'
lastSaved: '2026-06-22'
workflowType: 'testarch-atdd'
storyId: '1.5'
storyKey: '1-5-polyglot-eval-seam'
generatedTestFiles:
  - 'packages/eval/polyglot-eval-roundtrip.test.ts'
---

# ATDD Checklist — Epic 1, Story 1.5: Polyglot Eval Seam (SC-1)

**Date:** 2026-06-22 · **Primary Test Level:** integration (subprocess) + contract · **Severity:** **T1 (KEYSTONE AC-F1-05)**

## Acceptance Criteria
1. Python eval workspace returns `EvalResult` that passes TS-side zod parse (AC-F1-05 KEYSTONE)
2. `packages/contracts/scripts/gen-pydantic.ts` generates pydantic from zod → JSON Schema in CI
3. `tools/eval` has `pyproject.toml`, uv config, Dockerfile (containerized)
4. `tools/eval/package.json` is a shim (no JS, scripts shell to `uv run`)
5. `turbo.json` declares `py:*` tasks (`py:lint`, `py:test dependsOn py:lint`)
6. Invocation is subprocess/CLI, NOT HTTP (ADR-014)

## Red-Phase Scaffolds
**File:** `packages/eval/polyglot-eval-roundtrip.test.ts` (7 tests)

- ⏭️ tools/eval pyproject+Dockerfile — RED
- ⏭️ tools/eval package.json shim — RED (STR-12)
- ⏭️ turbo py:* tasks — RED
- ⏭️ subprocess not HTTP — RED (ADR-014)
- ⏭️ **EvalResult zod parse (KEYSTONE)** — RED — bridge+contract absent
- ⏭️ gen-pydantic codegen — RED
- ⏭️ schemaVersion on contract — RED

## Implementation Checklist

- [ ] `packages/eval/src/bridge.ts`: `runPythonEval(spec)` via `execa('uv', ['run', 'python', '-m', 'iip_eval', JSON.stringify(spec)])`
- [ ] `packages/contracts/src/eval.ts`: `EvalResult` zod schema with `schemaVersion` field
- [ ] `packages/contracts/scripts/gen-pydantic.ts`: zod → JSON Schema (via `zod-to-json-schema`) → pydantic via `datamodel-code-generator` with exact flags:
      `--field-constraints --use-union-operator --strict-types str,int,float,bool`
- [ ] `tools/eval/pyproject.toml` (Python 3.12, `requires-python`), `uv.lock` committed at workspace root
- [ ] `tools/eval/Dockerfile`: `FROM python:3.12-slim`, `uv` install
- [ ] `tools/eval/package.json` shim: `scripts.test → "uv run pytest"`, inline comment "shim — intentional non-standard (STR-12)"
- [ ] `turbo.json` v2 `tasks`: `py#lint`, `py#test` (dependsOn `py#lint`)
- [ ] Wire `gen-pydantic` to run in CI before `py:test`
- [ ] Pin RAGAS primary, DeepEval cross-check, Inspect agent-trace (project-context.md)
- [ ] Activate `test.skip` → GREEN

**Estimated Effort:** 2–3 days

## Notes
- **AC-F1-05 is the KEYSTONE of Story 1.5** — the polyglot bridge is defamation-load-bearing: Python eval drives the libel-injection / republication-framing evals (SEC-8) that have no TS mutation coverage otherwise.
- `krippendorff` vs `simpledorff`: pick ONE and pin (project-context.md) — α implementations disagree on missing-data handling.
- Subprocess contract schema-version is the bisect primitive — without it a Python minor lift silently breaks the gate.
- DO NOT hand-write pydantic models that mirror zod schemas (SC-1) — generate from one source.

**Generated by BMad TEA Agent** — 2026-06-22
