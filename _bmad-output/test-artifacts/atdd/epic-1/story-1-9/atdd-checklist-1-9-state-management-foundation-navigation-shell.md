---
stepsCompleted: ['step-01-preflight-and-context','step-02-generation-mode','step-03-test-strategy','step-04-generate-tests']
lastStep: 'step-04-generate-tests'
lastSaved: '2026-06-22'
workflowType: 'testarch-atdd'
storyId: '1.9'
storyKey: '1-9-state-management-foundation-navigation-shell'
generatedTestFiles:
  - 'apps/web/lib/state/state-navigation.test.ts'
---

# ATDD Checklist — Epic 1, Story 1.9: State Mgmt Foundation & Navigation Shell (UX-DR28-34/43)

**Date:** 2026-06-22 · **Primary Test Level:** component + unit · **Severity:** T2

## Acceptance Criteria
1. React Query 5.x configured with `lib/api.ts` (one HTTP wrapper, AbortController + retry, lint bans raw fetch) (UX-DR31)
2. Zustand stores exist: graph-store, timeline-store, chat-store, citation-store (UX-DR29)
3. nuqs configured with `lib/state/url-keys.ts` (single registry: ?seed=, ?renderer=, ?active=, ?mode=, ?from=, ?to=, ?q=) (UX-DR30)
4. Left sidebar: icons+labels on xl, icon-rail on lg, Sheet on md/below (UX-DR32)
5. Top bar: essence sentence (truncated), dark-mode toggle, command palette entry (UX-DR32)
6. Skip-to-content link is the first focusable element on every surface (UX-DR43)

## Red-Phase Scaffolds
**File:** `apps/web/lib/state/state-navigation.test.ts` (9 tests)

- ⏭️ url-keys.ts single registry — RED
- ⏭️ parsers typed — RED
- ⏭️ lib/api.ts AbortController + retry — RED
- ⏭️ ESLint bans raw fetch — RED
- ⏭️ 4 Zustand stores — RED
- ⏭️ skip-to-content first focusable — RED (WCAG UX-DR43)
- ⏭️ sidebar responsive — RED
- ⏭️ top bar — RED
- ⏭️ React Query wired — RED

## Implementation Checklist

- [ ] `lib/state/url-keys.ts`: nuqs `useQueryState` parsers for all 7 params (STR-10 single-registry)
- [ ] `lib/api.ts`: single HTTP wrapper — `apiFetch(input, { signal, retry })`, lint-ban raw `fetch` elsewhere
- [ ] `lib/state/{graph,timeline,chat,citation}-store.ts`: Zustand 5.x stores (ephemeral interaction)
- [ ] `app/providers.tsx`: `QueryClientProvider` with default staleTime/retry
- [ ] `components/layout/sidebar.tsx`: responsive `xl:` labels, `lg:` icon-rail, md/below `Sheet` (shadcn)
- [ ] `components/layout/top-bar.tsx`: essence truncated, theme toggle, cmdk palette entry
- [ ] `app/layout.tsx`: skip-to-content link as first `<a>` + `<main id="content">`
- [ ] ESLint `no-restricted-syntax` banning `fetch(` outside `lib/api.ts`
- [ ] Activate `test.skip` → GREEN

**Estimated Effort:** 2 days

## Notes
- URL is a public API for journalists — every param name/parser in ONE file (STR-10).
- State split: React Query (server) + Zustand (ephemeral) + nuqs (URL-shareable) — don't blur.
- Skip-to-content (UX-DR43) is WCAG 2.1 AA non-negotiable for keyboard users.

**Generated by BMad TEA Agent** — 2026-06-22
