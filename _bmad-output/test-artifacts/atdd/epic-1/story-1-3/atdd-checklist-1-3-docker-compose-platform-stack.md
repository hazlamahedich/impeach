---
stepsCompleted: ['step-01-preflight-and-context', 'step-02-generation-mode', 'step-03-test-strategy', 'step-04-generate-tests']
lastStep: 'step-04-generate-tests'
lastSaved: '2026-06-22'
workflowType: 'testarch-atdd'
storyId: '1.3'
storyKey: '1-3-docker-compose-platform-stack'
storyFile: '_bmad-output/planning-artifacts/epics.md (Story 1.3)'
atddChecklistPath: '_bmad-output/test-artifacts/atdd/epic-1/story-1-3/atdd-checklist-1-3-docker-compose-platform-stack.md'
generatedTestFiles:
  - 'tests/integration/compose-stack.health.test.ts'
---

# ATDD Checklist — Epic 1, Story 1.3: Docker Compose Platform Stack

**Date:** 2026-06-22 · **Primary Test Level:** integration (compose) · **Severity:** T2

## Acceptance Criteria
1. `docker compose up` brings all services healthy: postgres+AGE+pgvector, redis, minio, ollama, caddy, api, ingest-worker, serve-worker, audit-worker, enqueuer, web
2. Caddyfile: auto-TLS + rate-limit template (D9)
3. Ollama pre-pulls via `infra/runner/ollama-pull.sh` (D15)
4. MinIO private bucket for raw snapshots
5. Redis configured for BullMQ + Streams
6. OTel + Prometheus + Grafana wired (NFR-O-1)

## Red-Phase Scaffolds
**File:** `tests/integration/compose-stack.health.test.ts` (8 tests, all `.skip`)

- ⏭️ compose file + 11 services — RED — file absent
- ⏭️ services healthy — RED — depends on compose + 1.1 app stubs
- ⏭️ Caddyfile auto-TLS + rate-limit — RED — infra/Caddyfile absent
- ⏭️ ollama-pull.sh — RED — script absent
- ⏭️ MinIO bucket — RED — bucket init absent
- ⏭️ Redis ping — RED — compose absent
- ⏭️ OTel/Prometheus/Grafana services — RED — absent

## Implementation Checklist

- [ ] Author `infra/docker-compose.yml` with all 11 services + healthchecks
- [ ] Use the same SHA-pinned PG+AGE+pgvector image from Story 1.2 (SHARED)
- [ ] Ollama container with model pre-pulled to named volume (`qwen3:14b` per ADR-005)
- [ ] Caddy 2.8.x with auto-ACME + rate_limit directive (rate-limit for OWASP noise ONLY — SEC-9, NOT DDoS defense)
- [ ] `infra/runner/ollama-pull.sh` running `ollama pull qwen3:14b` (verify exact tag spelling vs ADR-005)
- [ ] MinIO bucket `raw-snapshots` created on startup (private, off serving path)
- [ ] OTel collector + Tempo (NOT Jaeger per project-context.md VAL-9) + Prometheus + Grafana
- [ ] App stubs (from 1.1) wired as compose services with healthchecks
- [ ] `docker compose up --wait` reaches healthy in <3 min
- [ ] Activate `test.skip` → GREEN

**Estimated Effort:** 2 days

## Notes
- Caddy rate-limit is OWASP-noise ONLY (SEC-9) — do NOT list as DDoS defense against state-aligned actors.
- Redis image MUST be `redis:7-alpine` only (Dragonfly/KeyDB have BullMQ bugs per project-context.md).
- Compose is TRANSITIONAL (AC-3 single-workstation); all deps behind interfaces per SC-5.
- Depends on Story 1.1 app stubs + Story 1.2 PG image.

**Generated by BMad TEA Agent** — 2026-06-22
