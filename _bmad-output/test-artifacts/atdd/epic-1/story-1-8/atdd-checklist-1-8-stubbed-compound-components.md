---
stepsCompleted: ['step-01-preflight-and-context','step-02-generation-mode','step-03-test-strategy','step-04-generate-tests']
lastStep: 'step-04-generate-tests'
lastSaved: '2026-06-22'
workflowType: 'testarch-atdd'
storyId: '1.8'
storyKey: '1-8-stubbed-compound-components'
generatedTestFiles:
  - 'apps/web/components/iip/citation/citation-empty-chip.test.tsx'
  - 'apps/web/components/iip/claim/claim-variants.test.tsx'
  - 'apps/web/components/iip/trust-badge/trust-badge.test.tsx'
  - 'apps/web/components/iip/source-verb-answer-empty.test.tsx'
---

# ATDD Checklist — Epic 1, Story 1.8: Stubbed Compound Components (UX-DR9-12/18/20)

**Date:** 2026-06-22 · **Primary Test Level:** component (@testing-library/react) · **Severity:** **T1 (AC-2 at component boundary)**

> This is the demoable spine of Epic 1 — a human can see the citation-or-silence invariant surface for the first time.

## Acceptance Criteria
1. `<Citation>` renders `<Citation.Empty>` by default, promotes to `<Citation.Chip>` only when provenance resolves (UX-DR9, AC-2 at component boundary)
2. `<Claim>` renders variant="fact" (solid), "attributed" (dashed+italic), "dashed" (strikethrough) with aria-label prefix (UX-DR10/36)
3. `<TrustBadge>` renders verified/contradicted/caution — always paired with icon + label (UX-DR11)
4. `<SourceVerbTag>` renders label-caps in primary, with `source-verbs.ts` registry (UX-DR12)
5. `<AnswerBlock>` renders answer-block (3px border), silence, essence (UX-DR18)
6. `<EmptyState>` renders display-sm headline + body-md body (UX-DR20)
7. All components live in `components/iip/` (separate from `components/ui/` shadcn)
8. `CitationContext` provider at root layout (UX-DR9)

## Red-Phase Scaffolds (14 tests across 4 files)

### `citation-empty-chip.test.tsx` (3 tests)
- ⏭️ Empty by default — RED — component absent
- ⏭️ Chip on provenance — RED
- ⏭️ Chip = role=link with aria-label (WCAG) — RED

### `claim-variants.test.tsx` (5 tests)
- ⏭️ fact solid border — RED
- ⏭️ attributed dashed+italic — RED
- ⏭️ dashed strikethrough — RED
- ⏭️ aria-label prefix — RED
- ⏭️ **REFUSES zero citations (AC-2 "no citation no claim")** — RED — defamation primitive

### `trust-badge.test.tsx` (3 tests via it.each)
- ⏭️ verified/contradicted/caution with icon+label — RED (color-only forbidden)

### `source-verb-answer-empty.test.tsx` (5 tests)
- ⏭️ SourceVerbTag label-caps — RED
- ⏭️ source-verbs.ts registry with bias+floor (EI-3 binding) — RED
- ⏭️ unregistered verb → fallback + warn — RED
- ⏭️ AnswerBlock 3px border / silence / essence — RED
- ⏭️ EmptyState display-sm headline — RED

## Implementation Checklist

- [ ] `components/iip/citation/` compound: `Citation`, `Citation.Empty`, `Citation.Chip`, `CitationContext` provider
- [ ] `components/iip/claim/`: `<Claim>` with `variant: 'fact'|'attributed'|'dashed'`, `citations: CitationRef[]`
- [ ] **Mechanical "no citation, no claim":** if `citations.length === 0`, render `<TrustBadge tier="insufficient">No sources — not shown</TrustBadge>` + `display:none` (PD-1 essence)
- [ ] `components/iip/trust-badge/`: 3 channels (color via semantic token + icon + text label), `role="img"`, `aria-label="Verified — N sources"`
- [ ] `components/iip/source-verb-tag/` + `lib/citation/source-verbs.ts`: registry with `verb`, `bias` (raise/lower/neutral), `variant`, `floor` (primary/secondary)
- [ ] `components/iip/answer-block/`: `AnswerBlock`, `AnswerBlock.Silence`, `AnswerBlock.Essence`
- [ ] `components/iip/empty-state/`: `display-sm` headline, `body-md` body
- [ ] `CitationContext` at root layout (`app/layout.tsx`)
- [ ] **Composition direction:** `components/iip/*` composes `components/ui/*`, NEVER reverse
- [ ] Activate `test.skip` → GREEN

**Estimated Effort:** 3 days

## Notes
- **Story 1.8 is where AC-2 becomes mechanical at the component boundary** — STR-8/STR-10. The `<Claim>` "no citation no claim" test is defamation-load-bearing.
- Citation is a COMPOUND primitive, NOT a shadcn atom (encodes domain invariants). Keep `components/iip/` separate from `components/ui/` so shadcn upgrades stay clean.
- `<Citation.Chip>` MUST be `role="link"` (never `<span>`) — the citation IS the verification path.
- All semantic tokens referenced via STR-10 tokens (Story 1.7 dependency).

**Generated by BMad TEA Agent** — 2026-06-22
