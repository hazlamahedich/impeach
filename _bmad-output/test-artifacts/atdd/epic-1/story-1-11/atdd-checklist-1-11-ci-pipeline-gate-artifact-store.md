---
stepsCompleted: ['step-01-preflight-and-context','step-02-generation-mode','step-03-test-strategy','step-04-generate-tests']
lastStep: 'step-04-generate-tests'
lastSaved: '2026-06-22'
workflowType: 'testarch-atdd'
storyId: '1.11'
storyKey: '1-11-ci-pipeline-gate-artifact-store'
generatedTestFiles:
  - 'tests/ci/ci-pipeline.test.ts'
---

# ATDD Checklist — Epic 1, Story 1.11: CI Pipeline & Gate Artifact Store (AR-20/22)

**Date:** 2026-06-22 · **Primary Test Level:** integration (workflow YAML + tooling) · **Severity:** T2

## Acceptance Criteria
1. `.github/workflows/ci.yml` runs build/test/lint/typecheck/eval/chaos/adr-lint (AC-F1-07)
2. Branch protection blocks merge on red; hard gates non-relaxable
3. Self-hosted runner provisioned separately from corpus workstation (SEC-4, `infra/runner/provision.pkr.hcl`)
4. `eval/gates/<corpus-hash>/` content-addressed structure (SC-7)
5. Corpus freeze primitive emits SHA-256 `eval/corpus/<hash>/manifest.json` (AC-F1-10)
6. Gate-time re-run emits `eval/gates/<hash>/decision.json` with pass/fail + per-metric (AC-F1-10)
7. sops 3.x + age 1.x configured; process refuses on invalid config (D7, NFR-S-4)

## Red-Phase Scaffolds
**File:** `tests/ci/ci-pipeline.test.ts` (9 tests)

- ⏭️ ci.yml task coverage — RED
- ⏭️ branch protection (enforce_admins, strict) — RED (AC-2 non-relaxable)
- ⏭️ runner isolated from corpus (SEC-4) — RED
- ⏭️ eval/gates content-addressed — RED (SC-7)
- ⏭️ corpus freeze manifest — RED (AC-F1-10)
- ⏭️ gate decision.json — RED (AC-F1-10)
- ⏭️ eval reproduce <run-id> bisect primitive — RED (SC-7)
- ⏭️ sops+age + refuse-on-invalid — RED
- ⏭️ OIDC ≤1h tokens — RED (SEC-4)

## Implementation Checklist

- [ ] `.github/workflows/ci.yml`: parallel jobs for TS lint / Python lint / markdown / xref-lint / adr-lint (one fail doesn't block another lane)
- [ ] `.github/branch-protection.json`: `required_status_checks.strict=true`, `enforce_admins.enabled=true` (SEC-6), `dismiss_stale_reviews=true`
- [ ] `infra/runner/provision.pkr.hcl`: Packer template for isolated self-hosted runner (NOT corpus workstation)
- [ ] `eval/gates/` content-addressed: append-only, supersede never overwrite, each artifact links to predecessor by hash
- [ ] `iip-eval freeze` → `eval/corpus/sha256:<hash>/manifest.json` (corpus SHA)
- [ ] `iip-eval reproduce <run-id>` → reconstructs `(corpus SHA, gate SHA, model digest, harness SHA)`, emits `decision.json`
- [ ] `.sops.yaml` with age backend; `iip-config validate --strict` exits ≠0 on invalid secrets
- [ ] CI uses `permissions: id-token: write` (OIDC), tokens ≤3600s — no persistent sops keys at runtime
- [ ] Linux runners only (macOS GitHub runners lack Docker per project-context.md)
- [ ] CI shards + `c8 merge` (NOT `nyc merge`) + `--detectOpenHandles`
- [ ] Activate `test.skip` → GREEN

**Estimated Effort:** 3 days

## Notes
- **The re-run command is the bisect primitive** (SC-7). Content-addressing without `eval reproduce <run-id>` is just file naming.
- Branch protection `enforce_admins: true` is non-negotiable for SEC-6 — admins can't override hard gates.
- The triple-lock: every eval run records `(corpus_version, model_version, gate_version)` as composite key. Comparisons across mismatched keys are invalid.

**Generated by BMad TEA Agent** — 2026-06-22
