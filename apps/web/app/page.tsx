/**
 * Minimal root page — required for `next build` under the App Router.
 * Story 1.8 will replace this with the real application shell.
 */
export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="font-display text-2xl">Impeachment Intelligence Platform</p>
    </main>
  );
}
